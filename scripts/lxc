#! /bin/bash

#### Script to attempt to create an unprivileged lxc container for the unprivileged user
#### I don't believe this is currently working

/bin/su -c "lxc-create -t download -n c1 -l DEBUG -o /home/unprivileged/log.txt" unprivileged
